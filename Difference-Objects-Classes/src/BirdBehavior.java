public enum BirdBehavior {

    /** This enum sets the values that can be applied to the
     * BirdBehavior variable of the Bird object. */
    FLYING,
    SINGING,
    EATING

} /* End of BirdBehavior */
